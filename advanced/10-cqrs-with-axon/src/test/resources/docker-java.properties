# Overrides the docker-java default API version (v1.24).
# Docker Desktop 29+ returns HTTP 400 for the legacy /v1.24/info endpoint.
# Setting 1.44 ensures Testcontainers can connect to Docker Desktop 29+.
api.version=1.44
