# Testcontainers configuration — complements docker-java.properties.
# Both files together ensure Testcontainers uses the Docker Desktop 29+ compatible API version.
DOCKER_API_VERSION=1.44
docker.api.version=1.44
