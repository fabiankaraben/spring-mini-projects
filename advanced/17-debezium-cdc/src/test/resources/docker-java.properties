# Overrides docker-java's default API version (v1.24).
# Docker Desktop 29+ rejects requests to /v1.24/info with HTTP 400.
# Setting 1.44 here fixes Testcontainers on Docker Desktop 29+.
api.version=1.44
