# Default configuration for the 'client-app' application.
# These properties are returned when no specific profile is requested,
# or as fallback values when a specific profile doesn't override them.
user.role=Developer
feature.toggles.beta=false
app.welcome.message=Welcome to the Default Environment
