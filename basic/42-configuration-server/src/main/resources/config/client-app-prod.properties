# Configuration for the 'prod' profile of 'client-app'.
# These values override the defaults in client-app.properties when the 'prod' profile is active.
user.role=User
feature.toggles.beta=false
app.welcome.message=Welcome to the Production Environment
logging.level.root=WARN
