package com.example.envconfig;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main class for the Environment Variable Config application.
 * This class serves as the entry point for the Spring Boot application.
 */
@SpringBootApplication
public class EnvironmentVariableConfigApplication {

    public static void main(String[] args) {
        SpringApplication.run(EnvironmentVariableConfigApplication.class, args);
    }
}
